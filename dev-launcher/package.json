{
  "name": "xcelerate-dev-launcher",
  "version": "1.0.0",
  "description": "Local dev control panel for Xcelerate",
  "main": "dist/main.js",
  "scripts": {
    "build": "tsc",
    "start": "pkill -f 'electron.*dev-launcher' 2>/dev/null; npm run build && concurrently --kill-others --names tsc,electron \"tsc --watch --preserveWatchOutput\" \"electron .\"",
    "watch": "tsc --watch"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "concurrently": "^9.2.1",
    "electron": "^29.0.0",
    "electron-reload": "^2.0.0-alpha.1",
    "typescript": "^5.3.0"
  }
}
